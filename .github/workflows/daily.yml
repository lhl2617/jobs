name: Daily cron jobs 

on:
  schedule:
    - cron: "0 0 * * *" # every day at midnight

jobs:
  backup-sukusukuseparuh:
    name: Backup SukuSukuSeparuh
    runs-on: ubuntu-latest
    steps:
      - name: Run script
        run: ./scripts/backup-sukusukuseparuh.sh
        env:
          GH_ACCESS_TOKEN: ${{ secrets.GH_ACCESS_TOKEN }}